<div class="modal fade" id="cancelModal" tabindex="-1" role="dialog" aria-labelledby="cancelModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Annulation de la proposition</h4>
            </div>
            <div class="modal-body">
                <form action="{{ path('submit.proposition') }}" id="formCancel" name="formCancel" method="post">
                    <div class="form-group">
                        <label for="commentaire">Motif du refus :</label>
                        <textarea class="form-control" name="commentaire" rows="3"></textarea>
                    </div>
                    <input name="idPreSession" type="hidden" value="{{ preSession.getId() }}">
                    <input name="action" type="hidden" type="hidden" value="cancel">
                    <button type="submit" class="btn btn-info">Valider</button>
                </form>
            </div>
            <div class="modal-footer">
                <em>Informations sous réserve</em>
            </div>
        </div>
    </div>
</div>